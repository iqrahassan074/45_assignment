let famous_person : string = "muhammad ali jinnah"
let quote : string = "think a hundred times before you take a dicision, but once that decision is taken, stand by as one man. expect the best, prepare for the worst."
let message : string = `${famous_person} once said "${quote}"` 
console.log(message);
